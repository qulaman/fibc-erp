# 📊 Табели и Навигация по Цехам

## ✅ Что сделано

### 1. Реорганизована Навигация

**До:** Один раздел "Производство" со всеми цехами в подменю

**После:** Каждый цех - отдельный пункт меню с собственным подменю:

```
🧵 Экструзия
  ├── Производство
  ├── Журнал
  └── Табель

🚜 Ткачество
  ├── Производство
  ├── Журнал
  └── Табель

🔆 Ламинация
  ├── Производство
  ├── Журнал
  └── Табель

🎗️ Стропы
  ├── Производство
  ├── Журнал
  └── Табель

✂️ Крой
  ├── Производство
  ├── Журнал
  └── Табель

🧷 Пошив
  ├── Производство
  ├── Табель
  └── Спецификации
```

### 2. Создан Табель для всех цехов

Для каждого цеха создана страница табеля (графика смен):

- ✅ `/production/extrusion/timesheet` - Табель экструзии
- ✅ `/production/weaving/timesheet` - Табель ткачества
- ✅ `/production/lamination/timesheet` - Табель ламинации
- ✅ `/production/straps/timesheet` - Табель строп
- ✅ `/production/cutting/timesheet` - Табель кроя
- ✅ `/production/sewing/timesheet` - Табель пошива

## 📋 Функциональность Табеля

### Основные возможности:

1. **Календарь на месяц** - отображает все дни текущего месяца
2. **Навигация по месяцам** - стрелки для переключения между месяцами
3. **Список сотрудников** - автоматически загружает всех активных сотрудников
4. **Отметка посещаемости** - клик по ячейке для добавления/удаления отметки
5. **Типы смен**:
   - 🌞 **День** (желтый цвет)
   - 🌙 **Ночь** (синий цвет)
6. **Настройка часов** - указание количества отработанных часов
7. **Итоги** - автоматический подсчет общих часов по сотруднику
8. **Экспорт в Excel** - выгрузка табеля в CSV формат

### Как использовать:

1. **Откройте нужный цех** (например, 🧵 Экструзия)
2. **Перейдите в "Табель"**
3. **Настройте "кисть"** (нижняя панель):
   - Выберите тип смены (День/Ночь)
   - Укажите количество часов (12, 8, и т.д.)
4. **Кликайте по ячейкам** для отметки:
   - Первый клик - добавляет отметку с настроенными параметрами
   - Второй клик - удаляет отметку
5. **Экспорт** - кнопка Download для выгрузки в Excel

### Визуальные индикаторы:

- 🟡 **Желтая ячейка** - дневная смена
- 🔵 **Синяя ячейка** - ночная смена
- 🔴 **Красный текст** - выходные дни (суббота, воскресенье)
- 💜 **Фиолетовый фон** - сегодняшний день
- **Число в ячейке** - количество отработанных часов

## 🗃️ База Данных

Табель использует таблицу `employee_attendance`:

```sql
CREATE TABLE employee_attendance (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  employee_id UUID NOT NULL REFERENCES employees(id),
  date DATE NOT NULL,
  status TEXT DEFAULT 'present',
  hours NUMERIC(5,2) NOT NULL,
  shift_type TEXT NOT NULL, -- 'Day' | 'Night'
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(employee_id, date)
);
```

**Примечание:** Если таблица не создана, выполните миграцию:

```sql
-- Создать таблицу посещаемости
CREATE TABLE IF NOT EXISTS employee_attendance (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  employee_id UUID NOT NULL REFERENCES employees(id),
  date DATE NOT NULL,
  status TEXT DEFAULT 'present',
  hours NUMERIC(5,2) NOT NULL,
  shift_type TEXT NOT NULL CHECK (shift_type IN ('Day', 'Night')),
  created_at TIMESTAMP WITHOUT TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITHOUT TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(employee_id, date)
);

-- Индексы
CREATE INDEX IF NOT EXISTS idx_attendance_employee ON employee_attendance(employee_id);
CREATE INDEX IF NOT EXISTS idx_attendance_date ON employee_attendance(date);

-- RLS политики
ALTER TABLE employee_attendance ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "Enable all for authenticated users" ON employee_attendance;
CREATE POLICY "Enable all for authenticated users" ON employee_attendance
  FOR ALL TO authenticated USING (true) WITH CHECK (true);

-- Триггер для updated_at
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ language 'plpgsql';

DROP TRIGGER IF EXISTS update_employee_attendance_updated_at ON employee_attendance;
CREATE TRIGGER update_employee_attendance_updated_at
    BEFORE UPDATE ON employee_attendance
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();
```

## 🎨 Файловая Структура

```
app/production/
├── extrusion/
│   ├── page.tsx              # Производство экструзии
│   ├── history/page.tsx      # Журнал экструзии
│   └── timesheet/page.tsx    # 📊 Табель экструзии
│
├── weaving/
│   ├── page.tsx              # Производство ткачества
│   ├── history/page.tsx      # Журнал ткачества
│   └── timesheet/page.tsx    # 📊 Табель ткачества
│
├── lamination/
│   ├── page.tsx              # Производство ламинации
│   ├── history/page.tsx      # Журнал ламинации
│   └── timesheet/page.tsx    # 📊 Табель ламинации
│
├── straps/
│   ├── page.tsx              # Производство строп
│   ├── history/page.tsx      # Журнал строп
│   └── timesheet/page.tsx    # 📊 Табель строп
│
├── cutting/
│   ├── page.tsx              # Производство кроя
│   ├── history/page.tsx      # Журнал кроя
│   └── timesheet/page.tsx    # 📊 Табель кроя
│
└── sewing/
    ├── page.tsx              # Производство пошива
    ├── timesheet/page.tsx    # 📊 Табель пошива
    └── specs/page.tsx        # Спецификации
```

## 🔧 Техническая информация

### Компоненты используемые:
- `Card` - карточки для секций
- `Button` - кнопки навигации и действий
- `Input` - поле ввода количества часов
- `Loader2` - анимация загрузки
- Иконки Lucide: `Calendar`, `ChevronLeft`, `ChevronRight`, `Moon`, `Sun`, `Minus`, `Plus`, `Download`

### Зависимости:
- `@/lib/supabase` - клиент Supabase для работы с БД
- `@/components/ui/*` - UI компоненты
- React hooks: `useState`, `useEffect`

### Оптимизация:
- **Sticky columns** - колонка с именами сотрудников закреплена слева
- **Responsive дизайн** - адаптивная верстка для мобильных устройств
- **Анимации** - плавные переходы при добавлении/удалении отметок
- **Кэширование** - оптимистичные обновления UI перед запросами к БД

## 📈 Следующие шаги

1. ✅ Создать таблицу `employee_attendance` в Supabase (выполнить SQL миграцию выше)
2. ✅ Проверить работу табелей для каждого цеха
3. 🔄 Настроить роли сотрудников для правильной фильтрации (опционально)
4. 🔄 Добавить возможность массового заполнения (если нужно)
5. 🔄 Добавить отчеты по табелям (сводки, аналитика)

## 🐛 Известные ограничения

1. Все сотрудники отображаются в табеле (без фильтрации по цеху)
   - **Решение:** Добавить поле `department` в таблицу `employees`
2. Нет проверки пересечения смен
   - **Решение:** Добавить валидацию на бэкенде
3. Нет истории изменений табеля
   - **Решение:** Добавить аудит логи

---

**Автор:** Claude Code
**Дата:** 2026-01-27
**Версия:** 1.0
