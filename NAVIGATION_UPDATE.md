# 🧭 Обновление навигации

## Что изменилось

В панель навигации добавлены новые страницы:

### 1. Ламинация (в разделе "Производство")

**Путь:** `/production/lamination`

Добавлена в подменю "Производство" между "Ткачество" и "Спецификации".

**Структура меню:**
```
Производство
├── Экструзия
├── История экструзии
├── Ткачество
├── История ткачества
├── Ламинация ← НОВОЕ
└── Спецификации
```

### 2. Администрирование (новый раздел)

**Путь:** `/admin`

Добавлен новый раздел в основную навигацию с иконкой ShieldCheck (🛡️).

**Структура меню:**
```
Администрирование
└── Сотрудники
```

Страница `/admin` позволяет:
- ✅ Просматривать список всех сотрудников
- ✅ Добавлять новых сотрудников
- ✅ Редактировать данные сотрудников
- ✅ Назначать роли (оператор экструзии, ткач, оператор ламинации и т.д.)
- ✅ Активировать/деактивировать сотрудников

## Роли в системе

Добавлена новая роль: **Оператор Ламинации** (`operator_lamination`)

**Полный список ролей:**
1. Администратор (`admin`)
2. Зав. Складом (`manager_warehouse`)
3. Оператор Экструзии (`operator_extruder`)
4. Намотчик (Экструзия) (`operator_winder`)
5. Ткач (`operator_weaver`)
6. **Оператор Ламинации** (`operator_lamination`) ← НОВОЕ
7. Оператор Резки (`operator_cutting`)
8. Швея (`operator_sewing`)

## Файлы изменены

### [components/Navigation.tsx](components/Navigation.tsx)

**Изменения:**
1. Добавлены импорты иконок:
   ```typescript
   import { ShieldCheck, Layers } from 'lucide-react';
   ```

2. Добавлена ламинация в подменю производства:
   ```typescript
   { name: 'Ламинация', href: '/production/lamination' }
   ```

3. Добавлен раздел Администрирование:
   ```typescript
   {
     name: 'Администрирование',
     href: '/admin',
     icon: ShieldCheck,
     submenu: [
       { name: 'Сотрудники', href: '/admin' },
     ]
   }
   ```

### [app/admin/page.tsx](app/admin/page.tsx)

**Исправления:**
- Изменен импорт Select компонента с `simple-select` на `my-select`
- Файл переименован из `employees` в `page.tsx` для корректной работы Next.js роутинга

## Визуальное представление

### Навигационная панель (обновленная)

```
┌─────────────────────────────────┐
│ 🏠 Главная                      │
│ 🏭 Производство                 │
│   ├─ Экструзия                  │
│   ├─ История экструзии          │
│   ├─ Ткачество                  │
│   ├─ История ткачества          │
│   ├─ Ламинация        ← НОВОЕ   │
│   └─ Спецификации               │
│ 🏪 Склад                        │
│   ├─ Склад нити                 │
│   ├─ Склад ткани                │
│   ├─ Сырье                      │
│   └─ История операций           │
│ 📋 Заказы            (Скоро)    │
│ 👥 Клиенты           (Скоро)    │
│ 📊 Отчеты            (Скоро)    │
│ 🛡️  Администрирование  ← НОВОЕ  │
│   └─ Сотрудники                 │
└─────────────────────────────────┘
```

## Как использовать

### Доступ к ламинации

1. Откройте панель навигации слева
2. Нажмите на раздел "Производство"
3. В подменю выберите "Ламинация"

### Доступ к управлению сотрудниками

1. Откройте панель навигации слева
2. Нажмите на раздел "Администрирование"
3. В подменю выберите "Сотрудники"

или прямо перейдите по URL: `/admin`

## Расширение системы

### Добавление новых подразделов в Администрирование

В будущем можно добавить:

```typescript
{
  name: 'Администрирование',
  href: '/admin',
  icon: ShieldCheck,
  submenu: [
    { name: 'Сотрудники', href: '/admin' },
    { name: 'Роли и права', href: '/admin/roles' },        // Будущее
    { name: 'Настройки системы', href: '/admin/settings' }, // Будущее
    { name: 'Логи', href: '/admin/logs' },                  // Будущее
  ]
}
```

### Добавление новых ролей

Отредактируйте массив `ROLES` в [app/admin/page.tsx](app/admin/page.tsx):

```typescript
const ROLES = [
  // ... существующие роли
  { value: 'new_role', label: 'Новая Роль' }, // Добавить здесь
];
```

## Проверка работоспособности

1. ✅ Запустите проект: `npm run dev`
2. ✅ Откройте навигацию
3. ✅ Убедитесь что раздел "Производство" содержит "Ламинация"
4. ✅ Убедитесь что есть раздел "Администрирование"
5. ✅ Перейдите на `/admin` и проверьте что страница загружается
6. ✅ Перейдите на `/production/lamination` и проверьте работу

## Troubleshooting

### Ошибка: "Module not found: Can't resolve '@/components/ui/simple-select'"

**Решение:** Уже исправлено. Импорт изменен на `my-select`.

### Страница /admin не загружается

**Проверьте:**
- Файл должен называться `app/admin/page.tsx` (не `employees`)
- Проверьте что Supabase таблица `employees` существует

### Ламинация не отображается в меню

**Проверьте:**
- Файл `components/Navigation.tsx` содержит запись о ламинации
- Очистите кэш браузера (Ctrl+Shift+R)
