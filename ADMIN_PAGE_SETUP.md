# 👥 Настройка страницы администрирования

## Проблема с ролью "Оператор Ламинации"

При попытке добавить сотрудника с ролью "Оператор Ламинации" возникает ошибка:

```
invalid input value for enum employee_role: "operator_lamination"
```

**Причина:** В базе данных существует ENUM тип `employee_role`, который не содержит значение `operator_lamination`.

## Решение

### Вариант 1: Добавить роль в базу данных (Рекомендуется)

Выполните SQL миграцию в Supabase SQL Editor:

```sql
-- Файл: supabase/add-lamination-role.sql

ALTER TYPE employee_role ADD VALUE IF NOT EXISTS 'operator_lamination';
```

**Шаги:**
1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Выполните файл [supabase/add-lamination-role.sql](supabase/add-lamination-role.sql)
4. Проверьте что роль добавлена:
   ```sql
   SELECT enumlabel FROM pg_enum WHERE enumtypid = 'employee_role'::regtype ORDER BY enumsortorder;
   ```

5. Раскомментируйте роль в коде [app/admin/page.tsx:21](app/admin/page.tsx:21):
   ```typescript
   // Было:
   // { value: 'operator_lamination', label: 'Оператор Ламинации' },

   // Станет:
   { value: 'operator_lamination', label: 'Оператор Ламинации' },
   ```

### Вариант 2: Не использовать роль ламинации (Временное решение)

Если вам не нужна роль ламинации прямо сейчас, можно оставить её закомментированной в коде (уже сделано).

## Текущее состояние

### Доступные роли (работают сейчас)

- ✅ Администратор (`admin`)
- ✅ Зав. Складом (`manager_warehouse`)
- ✅ Оператор Экструзии (`operator_extruder`)
- ✅ Намотчик (Экструзия) (`operator_winder`)
- ✅ Ткач (`operator_weaver`)
- ✅ Оператор Резки (`operator_cutting`)
- ✅ Швея (`operator_sewing`)

### Роли требующие миграции

- ⏳ Оператор Ламинации (`operator_lamination`) - закомментирована, требует миграции

## Как использовать страницу

После устранения проблемы с ENUM:

### 1. Открыть страницу
- URL: `/admin`
- Или через меню: "Администрирование → Сотрудники"

### 2. Добавить нового сотрудника
1. Нажмите кнопку "Добавить сотрудника"
2. Заполните форму:
   - **ФИО** - полное имя сотрудника
   - **Должность (Роль)** - выберите из выпадающего списка
3. Нажмите "Сохранить"

### 3. Редактировать сотрудника
1. Найдите карточку сотрудника
2. Нажмите кнопку "Изменить"
3. Отредактируйте данные
4. Нажмите "Сохранить"

### 4. Деактивировать/Активировать сотрудника
- Нажмите на иконку ⚡ (Power) в карточке сотрудника
- Деактивированные сотрудники отображаются полупрозрачными
- Деактивированные сотрудники не появляются в списках операторов в других модулях

### 5. Поиск
- Введите имя или должность в поле поиска
- Список автоматически отфильтруется

## Интеграция с другими модулями

Страница управления сотрудниками интегрирована с:

### Модуль экструзии
- Загружает только активных сотрудников (`is_active = true`)
- Фильтрация по ролям не применяется - можно выбрать любого активного сотрудника

### Модуль ткачества
- Загружает только активных сотрудников
- Можно назначить любого активного сотрудника

### Модуль ламинации (будущее)
- Будет использовать роль `operator_lamination` после миграции

## Структура данных

### Таблица `employees`

```sql
CREATE TABLE employees (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name TEXT NOT NULL,
  role employee_role NOT NULL,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### ENUM `employee_role`

**Текущие значения:**
```sql
CREATE TYPE employee_role AS ENUM (
  'admin',
  'manager_warehouse',
  'operator_extruder',
  'operator_winder',
  'operator_weaver',
  'operator_cutting',
  'operator_sewing'
);
```

**После миграции:**
```sql
-- Добавится:
'operator_lamination'
```

## Troubleshooting

### Ошибка: "invalid input value for enum employee_role"

**Причина:** Пытаетесь использовать роль, которой нет в ENUM

**Решение:**
1. Запустите миграцию `add-lamination-role.sql`
2. Или используйте существующие роли

### Сотрудники не загружаются

**Проверьте:**
1. Таблица `employees` существует в Supabase
2. В консоли браузера нет ошибок (F12 → Console)
3. Права доступа в Row Level Security настроены корректно

### Роль отображается на английском

**Решение:** Добавьте перевод в массив `ROLES`:

```typescript
const ROLES = [
  // ...
  { value: 'new_role', label: 'Новая Роль' },
];
```

### Нельзя изменить ENUM после добавления данных

**Проблема:** PostgreSQL не позволяет удалять значения из ENUM если они используются

**Решение:**
- Для добавления новых значений используйте `ALTER TYPE ... ADD VALUE`
- Для удаления значений нужно:
  1. Создать новый ENUM
  2. Мигрировать данные
  3. Удалить старый ENUM

## Дополнительные возможности (будущее)

В будущем можно добавить:

### 1. Детальная информация о сотруднике
- Фотография
- Контактные данные (телефон, email)
- Дата приема на работу
- График работы

### 2. Права доступа по ролям
- Разграничение доступа к модулям
- Роль `admin` имеет доступ ко всем функциям
- Операторы видят только свои модули

### 3. История изменений
- Кто и когда изменил данные сотрудника
- Аудит действий

### 4. Импорт/Экспорт
- Массовая загрузка сотрудников из Excel
- Экспорт списка в CSV

## Файлы проекта

- [app/admin/page.tsx](app/admin/page.tsx) - основная страница
- [supabase/add-lamination-role.sql](supabase/add-lamination-role.sql) - миграция для добавления роли
- [components/Navigation.tsx](components/Navigation.tsx) - навигация с разделом "Администрирование"
