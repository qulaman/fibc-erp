# 🔗 Интеграция модуля экструзии со справочником спецификаций

## Что изменилось

Модуль экструзии теперь использует уникальные значения денье из справочника спецификаций (`tkan_specifications`) вместо отдельной таблицы `yarn_types`. Это обеспечивает:

- ✅ Автоматическая синхронизация типов нити со спецификациями тканей
- ✅ Производство только той нити, которая используется в тканях
- ✅ Упрощение управления справочниками
- ✅ Единый источник данных для всей системы

## Как это работает

### 1. Извлечение денье из спецификаций

При загрузке страницы экструзии система:
1. Загружает все спецификации из `tkan_specifications`
2. Извлекает уникальные значения из полей `osnova_denye` и `utok_denye`
3. Сортирует по возрастанию денье
4. Формирует список доступных типов нити

**Пример:**
```
Спецификации:
- Ткань А: osnova=800D, utok=800D
- Ткань Б: osnova=1000D, utok=800D
- Ткань В: osnova=1200D, utok=1000D

Результат:
- Нить 800D (PP-800D)
- Нить 1000D (PP-1000D)
- Нить 1200D (PP-1200D)
```

### 2. Выбор нити при производстве

Оператор выбирает денье из списка:
- "Нить 800D (PP-800D)"
- "Нить 1000D (PP-1000D)"
- и т.д.

### 3. Запись в базу данных

При сохранении смены записываются:
- `yarn_denier` - значение денье (число)
- `yarn_code` - код нити (например: PP-800D)
- `yarn_name` - название (например: Нить 800D)

## Изменения в базе данных

### Новые поля в `production_extrusion`
```sql
yarn_denier INTEGER,      -- Денье нити
yarn_code VARCHAR(50),     -- Код нити (PP-800D)
yarn_name VARCHAR(255)     -- Название нити
```

### Новые поля в `yarn_inventory`
```sql
yarn_denier INTEGER,      -- Денье нити
yarn_code VARCHAR(50),     -- Код нити
yarn_name VARCHAR(255)     -- Название нити
```

## Миграция

### Шаг 1: Запустите SQL скрипт

В Supabase SQL Editor выполните:
```sql
-- Файл: supabase/extrusion-migrate-to-specs.sql
```

### Шаг 2: Проверьте наличие спецификаций

```sql
SELECT DISTINCT osnova_denye FROM tkan_specifications
UNION
SELECT DISTINCT utok_denye FROM tkan_specifications
ORDER BY 1;
```

Должны быть результаты (например: 800, 1000, 1200).

### Шаг 3: Проверьте работу

1. Откройте `/production/extrusion`
2. В списке нитей должны появиться денье из спецификаций
3. Создайте тестовую смену
4. Проверьте что данные записались корректно

## Преимущества новой системы

### 1. Автоматизация
- Добавили новую ткань с денье 1500D? Нить автоматически появится в экструзии
- Не нужно вручную создавать типы нити

### 2. Консистентность
- Невозможно произвести нить, которая не используется ни в одной ткани
- Единый справочник для всех модулов

### 3. Простота
- Меньше таблиц в базе данных
- Меньше дублирования данных

## Обратная совместимость

Старые поля (`yarn_type_id`) сохранены в базе данных, но не используются в коде.

### Если нужно вернуться к старой системе:
1. Раскомментируйте код загрузки `yarn_types`
2. Верните `yarn_type_id` в форму
3. Старые данные останутся в базе

### Если уверены что новая система работает:
```sql
-- Удалите старые поля
ALTER TABLE production_extrusion DROP COLUMN yarn_type_id;
ALTER TABLE yarn_inventory DROP COLUMN yarn_type_id;
```

## Примеры использования

### Создание новой смены

```typescript
// Старая версия
yarn_type_id: "uuid-нити-800D"

// Новая версия
yarn_denier: 800
yarn_code: "PP-800D"
yarn_name: "Нить 800D"
```

### Поиск на складе

```sql
-- Старая версия
SELECT * FROM yarn_inventory WHERE yarn_type_id = 'uuid';

-- Новая версия
SELECT * FROM yarn_inventory WHERE yarn_denier = 800;
-- или
SELECT * FROM yarn_inventory WHERE yarn_code = 'PP-800D';
```

## Интеграция с модулем ткачества

Модуль ткачества теперь может загружать партии нити по денье:

```typescript
// В ткачестве
const spec = {
  utok_denye: 800,
  osnova_denye: 1000
};

// Загрузка партий
loadYarnBatches(spec.utok_denye, 'weft');   // Найдет все партии 800D
loadYarnBatches(spec.osnova_denye, 'warp'); // Найдет все партии 1000D
```

## Поддержка

### Проблема: Список нитей пустой
**Решение:**
- Проверьте что таблица `tkan_specifications` создана
- Убедитесь что есть данные с заполненными `osnova_denye` и `utok_denye`

### Проблема: Ошибка при сохранении
**Решение:**
- Запустите миграцию `extrusion-migrate-to-specs.sql`
- Проверьте что новые поля созданы в таблицах

### Проблема: История не отображается корректно
**Решение:**
- Обновите страницу истории (уже обновлено в коде)
- Старые записи будут показывать `yarn_types?.name`
- Новые записи показывают `yarn_name` или `Нить ${yarn_denier}D`
