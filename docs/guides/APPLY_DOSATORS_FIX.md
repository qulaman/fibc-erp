# 🔧 Применение исправления для данных дозаторов

## Проблема
Данные дозаторов (загруженное сырье) не сохранялись в базу, поэтому материальный баланс показывал -100 кг.

## Что было исправлено

### 1. Функция `register_extrusion_output`
Добавлены новые параметры:
- `p_dosators_data JSONB` - данные всех дозаторов
- `p_output_bobbins INTEGER` - количество бобин
- `p_waste_weight NUMERIC` - вес отходов

### 2. Страница экструзии
Теперь передает данные дозаторов в функцию при регистрации партии.

## 📋 Как применить исправление

### Вариант 1: Через Supabase SQL Editor (быстро)

1. Откройте **Supabase SQL Editor**: https://supabase.com/dashboard/project/mgslapkswztsonyooogm/sql
2. Откройте файл на компьютере:
   `supabase/migrations/20250209_fix_register_extrusion_to_use_yarn_inventory.sql`
3. Скопируйте **ВСЁ** содержимое файла
4. Вставьте в SQL Editor
5. Нажмите **RUN** (Ctrl+Enter)
6. Должно появиться: **"Success. No rows returned"**

### Вариант 2: Через PowerShell

```powershell
# Скопируйте содержимое миграции
Get-Content "supabase\migrations\20250209_fix_register_extrusion_to_use_yarn_inventory.sql" | clip

# Затем вставьте в Supabase SQL Editor
```

## ✅ После применения

1. **Обновите страницу браузера** (Ctrl+R)
2. Откройте страницу экструзии
3. Заполните форму, **обязательно заполните дозаторы**
4. Нажмите "Выпустить Партию"
5. Откройте детальную карточку в журнале производства
6. **Теперь должны отображаться:**
   - ✅ Загруженное сырье (данные дозаторов)
   - ✅ Общий вес сырья
   - ✅ Правильный материальный баланс

## 🔍 Проверка

После создания новой партии, материальный баланс должен быть близок к нулю:

```
Вход (сырье):     100.0 кг
Выход (продукт):   95.0 кг
Выход (отходы):     5.0 кг
Баланс:             0.0 кг ✅
```

## ⚠️ Важно

Старые записи (созданные до применения исправления) не будут иметь данных дозаторов, так как эти данные не были сохранены. Новые записи будут корректными.
