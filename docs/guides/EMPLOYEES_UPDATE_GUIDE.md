# Обновление страницы сотрудников

## Что изменено

Страница управления персоналом была переделана с карточного отображения на **таблицу** с расширенными возможностями:

### ✨ Новые возможности

1. **Табличное отображение** - компактная таблица вместо карточек
2. **Поле "Цех"** - добавлена принадлежность к цеху/подразделению
3. **Inline редактирование** - можно редактировать прямо в таблице
4. **Расширенная фильтрация**:
   - По должности (роли)
   - По цеху
   - По статусу (активные/неактивные)
5. **Сортировка** по всем колонкам (циклическая: asc → desc → null)
6. **Быстрые действия**:
   - ✏️ Редактировать
   - 🔌 Активировать/Деактивировать
   - 🗑️ Удалить

### 📋 Колонки таблицы

| Колонка | Описание |
|---------|----------|
| **ФИО** | Полное имя сотрудника с аватаром-инициалом |
| **Должность** | Роль в системе (бейдж синего цвета) |
| **Цех** | Принадлежность к подразделению (бейдж оранжевого цвета) |
| **Статус** | Активен/Уволен (бейдж зелёный/красный) |
| **День рождения** | Дата с подсветкой ближайших дней рождения |
| **Действия** | Кнопки управления |

### 🎂 Дни рождения

- **Сегодня** - строка подсвечивается розовым, показывается 🎉
- **Через 1-7 дней** - выделяется розовым цветом
- **Остальные** - обычный серый текст
- **Сортировка** - можно отсортировать по близости дня рождения

---

## 🔧 Миграция БД (ВАЖНО!)

Для работы новой страницы нужно добавить поле `department` в таблицу `employees`.

### Шаг 1: Выполнить SQL-скрипт

Выполните файл [supabase/add-employee-department.sql](supabase/add-employee-department.sql) в вашей БД:

**Вариант A: Через Supabase Dashboard**

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в **SQL Editor**
3. Создайте новый запрос
4. Скопируйте содержимое `supabase/add-employee-department.sql`
5. Нажмите **Run**

**Вариант B: Через локальный psql**

```bash
psql $DATABASE_URL -f supabase/add-employee-department.sql
```

### Шаг 2: Обновить существующих сотрудников (опционально)

После добавления поля `department` можно обновить существующих сотрудников:

```sql
-- Пример автоматического назначения цехов по ролям
UPDATE employees SET department = 'extrusion' WHERE role LIKE '%extrud%';
UPDATE employees SET department = 'weaving' WHERE role LIKE '%weav%';
UPDATE employees SET department = 'lamination' WHERE role LIKE '%lamin%';
UPDATE employees SET department = 'straps' WHERE role LIKE '%strap%';
UPDATE employees SET department = 'cutting' WHERE role LIKE '%cutting%';
UPDATE employees SET department = 'sewing' WHERE role LIKE '%sewing%';
UPDATE employees SET department = 'warehouse' WHERE role LIKE '%warehouse%' OR role LIKE '%склад%';
UPDATE employees SET department = 'admin' WHERE role = 'admin';
```

Или назначить вручную через интерфейс (просто отредактируйте нужного сотрудника).

---

## 📖 Справочник цехов

Доступные значения для поля `department`:

| Код | Название |
|-----|----------|
| `extrusion` | Экструзия |
| `weaving` | Ткачество |
| `lamination` | Ламинация |
| `straps` | Стропы |
| `cutting` | Крой |
| `sewing` | Пошив |
| `warehouse` | Склад |
| `admin` | Администрация |
| `other` | Другое |

---

## 🎨 Использование

### Добавление сотрудника

1. Нажмите **"Добавить сотрудника"**
2. Заполните:
   - ФИО *
   - Должность * (можно создать новую роль)
   - Цех (выбор из списка)
   - Дата рождения (опционально)
3. Нажмите **"Сохранить"**

### Редактирование

**Способ 1: Inline (в таблице)**

1. Нажмите кнопку ✏️ **Редактировать**
2. Изменяйте поля прямо в строке
3. Нажмите ✅ **Сохранить** или ❌ **Отменить**

**Способ 2: Через модальное окно** (пока не реализовано, только inline)

### Деактивация сотрудника

1. Нажмите кнопку 🔌 рядом с сотрудником
2. Строка станет полупрозрачной
3. Статус изменится на "Уволен"
4. Повторное нажатие - активация обратно

### Удаление сотрудника

1. Нажмите кнопку 🗑️
2. Подтвердите действие
3. Сотрудник будет удалён из БД (**необратимо!**)

**⚠️ Внимание**: Удаление полностью стирает запись. Для временного скрытия используйте деактивацию.

---

## 🔍 Фильтрация и поиск

### Поиск

Введите текст в строку поиска - будет искаться по:
- ФИО сотрудника
- Должности (роли)

### Фильтры

- **Должность** - выпадающий список всех ролей в системе
- **Цех** - выпадающий список всех цехов
- **Статус** - кнопки: Все / Активные / Неактивные

Фильтры работают совместно (AND-логика).

### Сортировка

Нажмите на заголовок колонки:
- **1-й клик** - сортировка по возрастанию ↑
- **2-й клик** - сортировка по убыванию ↓
- **3-й клик** - отмена сортировки

Сортируются все колонки:
- ФИО (алфавит)
- Должность (алфавит)
- Цех (алфавит)
- День рождения (по близости)

---

## 🔗 Связанные файлы

- [app/admin/employees/page.tsx](app/admin/employees/page.tsx) - основная страница
- [app/admin/employees/EmployeesDataTable.tsx](app/admin/employees/EmployeesDataTable.tsx) - компонент таблицы
- [supabase/add-employee-department.sql](supabase/add-employee-department.sql) - SQL-миграция

---

## 📝 Технические детали

### Структура таблицы `employees`

```sql
CREATE TABLE employees (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name VARCHAR(255) NOT NULL,
  role VARCHAR(100) NOT NULL,
  department VARCHAR(50),          -- НОВОЕ ПОЛЕ
  birth_date DATE,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Индекс для поиска по цеху
CREATE INDEX idx_employees_department ON employees(department);
```

### TypeScript интерфейс

```typescript
interface Employee {
  id: string;
  full_name: string;
  role: string;
  department?: string; // Новое поле
  birth_date?: string;
  is_active: boolean;
  created_at?: string;
}
```

---

## ✅ Чек-лист внедрения

- [ ] Выполнить SQL-миграцию `add-employee-department.sql`
- [ ] Обновить существующих сотрудников (задать им цеха)
- [ ] Проверить работу фильтров
- [ ] Проверить inline редактирование
- [ ] Проверить добавление нового сотрудника
- [ ] Проверить сортировку по всем колонкам
- [ ] Проверить отображение дней рождения

---

## 🐛 Известные ограничения

1. **RLS (Row Level Security)** - убедитесь, что у пользователей есть права на `UPDATE` и `DELETE` для таблицы `employees`
2. **Поле department не обязательно** - можно оставлять пустым при добавлении
3. **Удаление роли** - если роль используется сотрудниками, она всё равно отображается в фильтре

---

Готово! 🎉 Страница сотрудников обновлена и готова к использованию.
