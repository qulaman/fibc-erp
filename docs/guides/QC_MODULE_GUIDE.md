# Модуль ОТК и Упаковки

## 📋 Описание

Модуль технического контроля качества (ОТК) обеспечивает проверку готовой продукции после пошива перед отправкой на склад готовой продукции.

## 🔄 Схема движения продукции

```
┌─────────────┐      ┌──────────────────────┐      ┌─────────┐      ┌────────────────────┐
│   Пошив     │ ───> │ Склад отшитой        │ ───> │   ОТК   │ ───> │ Склад готовой      │
│             │      │ продукции (буфер)    │      │         │      │ продукции          │
└─────────────┘      └──────────────────────┘      └─────────┘      └────────────────────┘
                            ↓                           ↓                    ↓
                     Полуфабрикаты              Проверка качества      Годная продукция
                     без проверки               Годные + Брак          для отгрузки
```

## 🎯 Основной функционал

### 1. Склад отшитой продукции
- **Назначение**: Промежуточный буфер между пошивом и ОТК
- **Операции**: Приход (из пошива), Расход (в ОТК)
- **Таблица**: `sewn_products_warehouse`
- **View остатков**: `view_sewn_products_balance`

### 2. Приёмка ОТК
- **Страница**: `/production/qc`
- **Функции**:
  - Выбор изделия из склада отшитой продукции
  - Указание количества годных и брака
  - Категории брака: Шов, Материал, Размер, Загрязнение, Прочее
  - Решения: Принято, Отклонено, На доработку
- **Автоматические операции**:
  1. Запись в журнал ОТК
  2. Списание со склада отшитой продукции
  3. Оприходование годных на склад готовой продукции

### 3. Журнал ОТК
- **Страница**: `/production/qc/history`
- **Данные**:
  - История всех приёмок
  - Статистика по годным/браку
  - Процент брака по изделиям
  - Информация о контролерах

## 📊 Структура базы данных

### Таблица `sewn_products_warehouse`
```sql
- id (UUID)
- doc_number (VARCHAR) - уникальный номер документа
- operation_date (DATE) - дата операции
- operation_type (VARCHAR) - "Приход" или "Расход"
- product_code, product_name, product_type
- quantity (INTEGER)
- source_doc_number - ссылка на документ-основание
- employee_name, notes, status
```

### Таблица `qc_journal`
```sql
- id (UUID)
- doc_number (VARCHAR) - номер документа ОТК
- inspection_date (DATE)
- inspector_name (VARCHAR) - контролер ОТК
- product_code, product_name, product_type
- quantity_good (INTEGER) - годных
- quantity_defect (INTEGER) - брак
- defect_reason (TEXT)
- defect_category (VARCHAR)
- decision (VARCHAR) - решение ОТК
- source_doc_number, notes, status
```

### View `view_sewn_products_balance`
Остатки на складе отшитой продукции (Приход - Расход)

### View `view_qc_statistics`
Статистика ОТК по изделиям (процент брака, количество проверок)

## 🔧 Установка модуля

### Шаг 1: Выполнить SQL-миграцию

Выполните файл [supabase/qc-module-schema.sql](supabase/qc-module-schema.sql):

**Через Supabase Dashboard:**
1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. SQL Editor → New query
3. Скопируйте содержимое `qc-module-schema.sql`
4. Run

**Или через psql:**
```bash
psql $DATABASE_URL -f supabase/qc-module-schema.sql
```

### Шаг 2: Добавить сотрудников ОТК

В справочнике сотрудников ([/admin/employees](app/admin/employees/page.tsx)):
1. Добавьте контролеров ОТК
2. Укажите цех: **qc** (или создайте цех "ОТК")
3. Статус: Активен

**Или через SQL:**
```sql
UPDATE employees SET department = 'qc' WHERE role LIKE '%отк%' OR role LIKE '%qc%';
```

### Шаг 3: Обновить модуль пошива (ВАЖНО!)

Модуль пошива необходимо изменить, чтобы он отправлял продукцию на **склад отшитой продукции** вместо **склада готовой продукции**.

**Файл для изменения**: `app/production/sewing/daily/page.tsx`

**Было:**
```typescript
// Оприходование на склад готовой продукции
await supabase.from('finished_goods_warehouse').insert([{
  // ...
}]);
```

**Должно быть:**
```typescript
// Оприходование на склад отшитой продукции
await supabase.from('sewn_products_warehouse').insert([{
  doc_number: docNumber,
  operation_date: productionDate,
  operation_type: 'Приход',
  product_code: operationData.code,
  product_name: operationData.name,
  product_type: operationData.category,
  quantity: quantityGood,
  source_doc_number: productionDoc,
  source_doc_type: 'Пошив',
  employee_name: masterName,
  shift: shift,
  notes: notes,
  status: 'Активно'
}]);
```

## 📋 Рабочий процесс

### 1. Пошив изделий
- Швея отшивает изделия
- Годные изделия попадают на **склад отшитой продукции**
- Брак фиксируется отдельно

### 2. Приёмка ОТК
- Контролер ОТК открывает `/production/qc`
- Выбирает изделие из склада отшитой продукции
- Проверяет качество
- Указывает:
  - Количество годных
  - Количество брака (если есть)
  - Причину брака (обязательно при браке)
  - Решение (Принято/Отклонено/На доработку)
- Проводит документ

### 3. Автоматические операции
При проведении приёмки ОТК система автоматически:
1. Создаёт запись в журнале ОТК (таблица `qc_journal`)
2. Списывает все изделия (годные + брак) со склада отшитой продукции
3. Оприходует **только годные** на склад готовой продукции

### 4. Результат
- Годные изделия на складе готовой продукции
- Брак зафиксирован в журнале ОТК
- Статистика по проценту брака доступна

## 📊 Отчёты и статистика

### Журнал ОТК
- Все проверки с детализацией
- Процент брака по каждому изделию
- Категории брака
- Решения контролеров

### Статистика (Dashboard - в разработке)
- Общий процент брака по предприятию
- Топ-5 изделий по браку
- Контролеры ОТК (эффективность)
- Динамика брака по месяцам

## 🎨 Интерфейс

### Навигация
```
✅ ОТК
  ├─ Приёмка ОТК → /production/qc
  └─ Журнал → /production/qc/history
```

### Цветовая индикация
- **Зелёный** - годные изделия
- **Красный** - брак
- **Оранжевый** - процент брака
- **Синий** - номера документов

## ⚠️ Важные моменты

1. **Обязательная проверка остатков**
   - Система проверяет наличие изделий на складе отшитой продукции
   - Нельзя принять больше, чем есть на складе

2. **Брак обязательно требует причину**
   - Если указан брак > 0, поле "Причина брака" обязательно

3. **Автоматическое движение**
   - Годные автоматически попадают на склад готовой продукции
   - Брак НЕ попадает на склад, только фиксируется в журнале

4. **Цех ОТК для сотрудников**
   - Контролеры должны иметь department = 'qc'
   - Иначе не будут отображаться в списке контролеров

## 🔗 Связанные файлы

### Страницы
- [app/production/qc/page.tsx](app/production/qc/page.tsx) - приёмка ОТК
- [app/production/qc/history/page.tsx](app/production/qc/history/page.tsx) - журнал

### SQL
- [supabase/qc-module-schema.sql](supabase/qc-module-schema.sql) - схема БД

### Навигация
- [components/Navigation.tsx](components/Navigation.tsx) - добавлен раздел ОТК

### Эталонный код
- [FIBC ERP/ОТК и модуль упаковки.txt](FIBC%20ERP/ОТК%20и%20модуль%20упаковки.txt) - старая версия

## ✅ Чек-лист внедрения

- [ ] Выполнить SQL-миграцию `qc-module-schema.sql`
- [ ] Добавить сотрудников с department = 'qc'
- [ ] Обновить модуль пошива (отправка на склад отшитой продукции)
- [ ] Протестировать полный цикл: Пошив → ОТК → Склад готовой продукции
- [ ] Проверить расчёт остатков на всех складах

---

Модуль готов к использованию! 🎉
