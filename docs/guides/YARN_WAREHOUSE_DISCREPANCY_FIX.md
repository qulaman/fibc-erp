# 🔍 Исправление расхождений в складе нити

## Проблема

Пользователь сообщил: "журнал операции склада нити и сам склад нити показывают разные данные"

**Две страницы показывают разные цифры:**
1. **Склад нити** (`/warehouse/yarn`) - показывает `yarn_inventory.quantity_kg`
2. **Журнал операций** (`/warehouse/yarn/history`) - вычисляет баланс из записей производства

## Причины расхождений

### 1. **Главная причина: Нить не списывалась до исправления**

До исправления в [WEAVING_YARN_CONSUMPTION_FIX.md](./WEAVING_YARN_CONSUMPTION_FIX.md):
- Расход нити **считался** при завершении рулона
- Но **НЕ списывался** из `yarn_inventory.quantity_kg`
- Партии нити терялись между сменами

**Результат:**
- Старые рулоны: расход посчитан → показывается в журнале
- Но `yarn_inventory` не обновлялся → склад показывает завышенный остаток

### 2. **Рулоны без партий нити**

Старые рулоны созданные до исправления могут иметь:
- `warp_batch_id = NULL`
- `weft_batch_id = NULL`

Эти рулоны:
- ✅ Показываются в производстве
- ✅ Расход рассчитывается в журнале (по длине × норма)
- ❌ Но нельзя определить из какой партии нить была использована
- ❌ В `yarn_inventory` списание не произошло

### 3. **Статус 'used' не учитывался**

**Было:** Журнал считал расход только для `status = 'completed'`
**Стало:** Журнал теперь считает `status IN ('completed', 'used')`

## Что было исправлено

### 1. Фильтр статусов в журнале операций

**Файл:** `app/warehouse/yarn/history/page.tsx`

**Изменение (строка 122-124):**
```typescript
// БЫЛО:
// Добавляем расход только для завершенных рулонов
rollConsumptions.forEach((roll, rollId) => {
  if (roll.status === 'completed' && roll.spec) {

// СТАЛО:
// Добавляем расход для завершенных и использованных рулонов
rollConsumptions.forEach((roll, rollId) => {
  if ((roll.status === 'completed' || roll.status === 'used') && roll.spec) {
```

### 2. SQL диагностика

**Файл:** `check_yarn_discrepancy.sql`

Создан SQL-скрипт который:
- Сравнивает баланс склада с расчетным балансом
- Показывает расхождения по каждой партии
- Находит рулоны без партий нити (проблемные)
- Выдает общую статистику

## Как проверить расхождение

### Шаг 1: Запустите диагностический SQL

1. Откройте Supabase SQL Editor
2. Скопируйте содержимое файла `check_yarn_discrepancy.sql`
3. Запустите запрос

**Вы получите 3 таблицы:**

#### Таблица 1: Детализация по партиям
```
Партия          | Баланс_Склад | Приход | Расход_Основа | Расход_Уток | Баланс_Расчет | РАСХОЖДЕНИЕ
----------------|--------------|--------|---------------|-------------|---------------|-------------
260122-1-EXT... | 450.50       | 500.00 | 30.20         | 19.30       | 450.50        | 0.00
260123-2-EXT... | 380.00       | 400.00 | 0.00          | 0.00        | 400.00        | -20.00
```

**Интерпретация:**
- `РАСХОЖДЕНИЕ = 0` → все в порядке
- `РАСХОЖДЕНИЕ > 0` → на складе БОЛЬШЕ чем должно (расход не списался)
- `РАСХОЖДЕНИЕ < 0` → на складе МЕНЬШЕ (списалось больше чем надо)

#### Таблица 2: Рулоны без партий
```
Номер_Рулона    | Статус    | Длина_м | Не_Списано_Основа_кг | Не_Списано_Уток_кг
----------------|-----------|---------|----------------------|--------------------
R-250109-W-001  | completed | 180.0   | 15.30                | 12.78
R-250108-W-015  | used      | 220.0   | 18.70                | 15.62
```

**Эти рулоны - источник проблемы:**
- Они использовали нить
- Расход рассчитан в журнале
- Но из-за отсутствия `warp_batch_id`/`weft_batch_id` нить не списалась

#### Таблица 3: Общая статистика
```
Показатель                           | Значение_кг
-------------------------------------|-------------
Общий баланс на складе               | 2450.50
Общий приход из экструзии            | 3000.00
Расход в ткачестве (расчет)          | 549.50
Расчетный баланс (Приход - Расход)   | 2450.50
РАСХОЖДЕНИЕ (Склад - Расчет)         | 0.00
```

### Шаг 2: Определите масштаб проблемы

Если `РАСХОЖДЕНИЕ` в Таблице 3 **не равно 0**, значит есть проблема.

**Пример:**
```
РАСХОЖДЕНИЕ = +50.5 кг
```
→ На складе на 50.5 кг БОЛЬШЕ, чем должно быть
→ Старые рулоны не списывали нить

## Решение

### Вариант 1: Ничего не делать (Рекомендуется)

**Если расхождение небольшое (< 5% от общего баланса):**
- Оставьте как есть
- С момента исправления (WEAVING_YARN_CONSUMPTION_FIX.md) все работает корректно
- Старые данные постепенно "выровняются"
- Новые рулоны списывают нить правильно

**Преимущества:**
- ✅ Не нужно вмешиваться в данные
- ✅ Нет риска ошибки
- ✅ Система работает корректно с текущего момента

### Вариант 2: Корректировка баланса вручную (Осторожно!)

**Если расхождение значительное (> 10% от общего баланса):**

Можно вручную скорректировать `yarn_inventory.quantity_kg` для проблемных партий.

**Пример:** Партия `260122-1-EXT-01` имеет расхождение +50.5 кг

```sql
-- 1. Проверьте текущий баланс
SELECT batch_number, quantity_kg FROM yarn_inventory
WHERE batch_number = '260122-1-EXT-01';

-- Результат: quantity_kg = 450.5

-- 2. Вычтите расхождение
UPDATE yarn_inventory
SET
  quantity_kg = quantity_kg - 50.5,  -- Вычитаем расхождение
  quantity = quantity_kg - 50.5,     -- Обновляем quantity тоже
  last_updated = NOW()
WHERE batch_number = '260122-1-EXT-01';

-- 3. Проверьте результат
SELECT batch_number, quantity_kg FROM yarn_inventory
WHERE batch_number = '260122-1-EXT-01';

-- Результат должен быть: quantity_kg = 400.0
```

**⚠️ ВАЖНО:**
- Делайте корректировку ТОЛЬКО если вы понимаете причину расхождения
- Проверьте расчеты перед выполнением UPDATE
- Сохраните резервную копию данных
- Документируйте изменения

### Вариант 3: Восстановление партий для старых рулонов (Сложно)

**Если нужна полная трассируемость:**

Можно попытаться восстановить `warp_batch_id` и `weft_batch_id` для старых рулонов.

```sql
-- Найти рулоны без партий
SELECT
  wr.id,
  wr.roll_number,
  wr.created_at,
  ts.nazvanie_tkani,
  ts.osnova_denye,
  ts.utok_denye
FROM weaving_rolls wr
LEFT JOIN tkan_specifications ts ON wr.fabric_spec_id = ts.id
WHERE wr.status IN ('completed', 'used')
  AND (wr.warp_batch_id IS NULL OR wr.weft_batch_id IS NULL)
ORDER BY wr.created_at;

-- Для каждого рулона найти подходящую партию нити:
-- 1. По дате создания (ближайшая по времени)
-- 2. По денье (должно совпадать с osnova_denye / utok_denye)
-- 3. С достаточным остатком на складе

-- Пример обновления (для конкретного рулона):
UPDATE weaving_rolls
SET
  warp_batch_id = 'UUID-партии-основы',
  weft_batch_id = 'UUID-партии-утка'
WHERE id = 'UUID-рулона';
```

**⚠️ НЕ РЕКОМЕНДУЕТСЯ:**
- Очень трудоемко (нужно вручную для каждого рулона)
- Риск ошибки высокий
- Результат приблизительный (угадываем партию)

## Проверка после исправления

### 1. Проверьте журнал операций

Откройте `/warehouse/yarn/history`:
- Убедитесь, что расход теперь учитывает рулоны со статусом 'used'
- Проверьте, что общий баланс совпадает со складом

### 2. Создайте новый рулон (тест)

1. Откройте `/production/weaving`
2. Создайте новый рулон с выбором партий нити
3. Запишите несколько смен
4. Завершите рулон (поставьте галочку "Снять рулон")
5. Проверьте:
   - ✅ В журнале операций появился расход
   - ✅ На складе нити уменьшился баланс
   - ✅ Баланс склада = Расчетный баланс в журнале

### 3. Запустите диагностику снова

Повторно запустите `check_yarn_discrepancy.sql`:
- Расхождение для новых рулонов должно быть = 0
- Старые расхождения останутся (если не корректировали)

## Итоговый результат

### Что исправлено:

1. ✅ **Журнал операций** теперь учитывает статус 'used'
2. ✅ **SQL диагностика** для поиска расхождений
3. ✅ **Новые рулоны** (после WEAVING_YARN_CONSUMPTION_FIX) списывают нить корректно

### Что осталось:

- Старые рулоны (до исправления) могут иметь расхождения
- Это нормально и не влияет на текущую работу системы
- При необходимости можно скорректировать вручную

## Рекомендации

1. **Запустите диагностику** (`check_yarn_discrepancy.sql`)
2. **Оцените масштаб** расхождения (Таблица 3)
3. **Если < 5%** → оставьте как есть
4. **Если > 10%** → рассмотрите ручную корректировку
5. **Документируйте** любые изменения балансов

## Технические детали

### Файлы изменены:

1. **app/warehouse/yarn/history/page.tsx**
   - Строка 122-124: Добавлен учет статуса 'used'

2. **check_yarn_discrepancy.sql** (новый файл)
   - Диагностические запросы
   - 3 таблицы результатов

### Связанные исправления:

- [WEAVING_YARN_CONSUMPTION_FIX.md](./WEAVING_YARN_CONSUMPTION_FIX.md) - Исправление списания нити при ткачестве

---

**Статус:** ✅ Исправление применено
**Дата:** 2025-02-09
**Влияние:** Журнал операций теперь показывает корректные данные
**Требуется действие:** Запустить диагностику и оценить масштаб расхождения
