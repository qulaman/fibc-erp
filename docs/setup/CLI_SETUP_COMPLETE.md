# ✅ Supabase CLI - Готов к настройке

## Что уже сделано:

✅ Supabase CLI установлен (через npx)
✅ Проект инициализирован (`config.toml` создан)
✅ Project ID найден: `mgslapkswztsonyooogm`
✅ Скрипты подключения созданы

---

## Что нужно сделать (3 простых шага):

### Шаг 1: Получите Database Password

1. Откройте https://supabase.com/dashboard
2. Выберите свой проект
3. Перейдите в **Settings** → **Database**
4. Найдите раздел **Database Password**
5. Скопируйте пароль (или нажмите **Reset Database Password** если забыли)

⚠️ **Сохраните пароль** - он понадобится один раз для подключения.

---

### Шаг 2: Подключите CLI к проекту

Выполните в терминале:

```bash
cd c:/Users/Turing/fibc-erp
bash link_project.sh
```

**Что произойдет:**
1. CLI попросит ввести database password
2. Введите пароль (символы не будут видны - это нормально)
3. Нажмите Enter
4. CLI подключится к вашему проекту

**Ожидаемый результат:**
```
✅ Finished supabase link.
```

---

### Шаг 3: Получите схему базы данных

После успешного подключения выполните:

```bash
bash dump_schema.sh
```

**Что произойдет:**
1. CLI выгрузит всю структуру БД
2. Создаст файл `supabase/current_schema.sql` (полная схема)
3. Создаст файл `supabase/tables_schema.sql` (только таблицы)

**Ожидаемый результат:**
```
✅ Schema saved to: supabase/current_schema.sql
✅ Tables schema saved to: supabase/tables_schema.sql
```

---

## После выполнения этих шагов:

✅ Я смогу **читать файл** `supabase/current_schema.sql`
✅ Я буду **видеть всю структуру** БД
✅ Я смогу **создавать правильные миграции** без дубликатов
✅ Вы сможете **обновлять схему** в любой момент командой `bash dump_schema.sh`

---

## Проверка успешности:

После Шага 3 выполните:

```bash
ls -lh supabase/current_schema.sql
```

Должен показаться файл размером >10KB.

---

## Если что-то пошло не так:

### Ошибка: "Password authentication failed"
- Проверьте правильность пароля
- Сбросьте пароль в Dashboard → Settings → Database

### Ошибка: "Project not found"
- Убедитесь, что Project ID правильный: `mgslapkswztsonyooogm`
- Проверьте, что вы вошли в правильный аккаунт

### Ошибка: "Permission denied"
- Выполните: `chmod +x link_project.sh dump_schema.sh`

---

## Команды для будущего использования:

### Обновить схему БД:
```bash
bash dump_schema.sh
```

### Создать новую миграцию:
```bash
npx supabase migration new migration_name
```

### Применить миграции:
```bash
npx supabase db push
```

---

## Следующий шаг:

После успешного получения схемы, я прочитаю файл `current_schema.sql` и создам **правильную миграцию** для добавления недостающих полей в `production_extrusion` без создания дубликатов.

**Готовы начать?** Выполните Шаг 1! 🚀
