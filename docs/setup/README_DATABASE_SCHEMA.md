# 🗃️ Как дать Claude доступ к полной схеме БД

## Проблема
Claude не видит структуру вашей базы данных, поэтому:
- ❌ Создает дублирующие таблицы (`yarn_warehouse`)
- ❌ Использует несуществующие поля
- ❌ Не знает о связях между таблицами

## Два варианта решения

---

## ⚡ ВАРИАНТ 1: Быстрый (5 минут)

### Шаг 1: Выполните SQL запрос
1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Откройте файл [`get_complete_schema.sql`](get_complete_schema.sql)
3. Скопируйте ВСЕ содержимое и вставьте в SQL Editor
4. Нажмите **RUN**

### Шаг 2: Сохраните результаты
1. Скопируйте ВСЕ результаты запроса (все блоки)
2. Создайте файл `supabase/schema_export.txt`
3. Вставьте туда результаты
4. Сохраните

### Шаг 3: Покажите схему Claude
```bash
# В следующем сообщении отправьте:
"Вот полная схема БД:"
# И прикрепите файл supabase/schema_export.txt
```

**Плюсы:** Быстро, работает сразу
**Минусы:** Нужно обновлять вручную после каждой миграции

---

## 🐋 ВАРИАНТ 2: Правильный (30 минут, один раз)

### Шаг 1: Установите Docker Desktop

**Автоматическая установка:**
```powershell
cd C:\Users\Turing\fibc-erp
.\scripts\install-docker.ps1
```

**Ручная установка:**
1. Скачайте: https://www.docker.com/products/docker-desktop/
2. Запустите установщик
3. Перезагрузите компьютер (если требуется)
4. Запустите Docker Desktop
5. Дождитесь зеленого значка в системном трее

**Проверка:**
```powershell
docker --version
# Должно показать: Docker version 24.x.x
```

### Шаг 2: Получите полный дамп схемы

```powershell
cd C:\Users\Turing\fibc-erp
npm run schema:dump
```

Это создаст файл `supabase/current_schema.sql` с полной структурой БД!

### Шаг 3: Автоматическое обновление

После каждой миграции просто выполняйте:
```powershell
npm run schema:dump
```

**Плюсы:** Автоматизировано, всегда актуально, полная информация
**Минусы:** Требует установки Docker

---

## 📊 Что будет в схеме?

После выполнения любого варианта, Claude увидит:
- ✅ Все таблицы (production_extrusion, employees, equipment, и т.д.)
- ✅ Все колонки с типами данных
- ✅ Первичные ключи
- ✅ Внешние ключи (связи между таблицами)
- ✅ Все функции (RPC) и их параметры
- ✅ Views и индексы

## 🎯 Рекомендация

**Для разовой проверки:** Вариант 1 (SQL запрос)
**Для постоянной работы:** Вариант 2 (Docker + автоматизация)

## 🚀 Что делать прямо сейчас?

### Вариант А: Быстрый старт
```powershell
# 1. Откройте Supabase SQL Editor
# 2. Вставьте содержимое get_complete_schema.sql
# 3. Нажмите RUN
# 4. Сохраните результаты в текстовый файл
# 5. Покажите файл Claude
```

### Вариант Б: Правильная настройка
```powershell
# 1. Установите Docker Desktop (ссылка выше)
# 2. После установки и перезагрузки:
cd C:\Users\Turing\fibc-erp
npm run schema:dump
# 3. Проверьте файл:
cat supabase\current_schema.sql
```

## ❓ Вопросы

**Q: Нужно ли обновлять схему после каждой миграции?**
A: Да, но только если вы меняете структуру БД (добавляете таблицы/колонки)

**Q: Docker занимает много места?**
A: Docker Desktop ~500 МБ + образы ~1-2 ГБ

**Q: Можно ли без Docker?**
A: Да, используйте Вариант 1 (SQL запрос), но придется обновлять вручную

---

## 🔧 Решение текущей проблемы с `yarn_warehouse`

После получения схемы мы увидим:
1. Существует ли таблица `yarn_warehouse`
2. Если нет - создадим миграцию
3. Если да - исправим функцию `register_extrusion_output`

**Следующий шаг:** Выберите вариант (1 или 2) и выполните его!
