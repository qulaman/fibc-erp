-- =====================================================
-- ПОЛНАЯ ОЧИСТКА СКЛАДА ОТШИТОЙ ПРОДУКЦИИ И ОТК
-- =====================================================
-- ⚠️ ВНИМАНИЕ: Это удалит ВСЕ данные из складов и журналов!
-- Используйте только если вы уверены!

-- 1. Удалить все записи из склада отшитой продукции
DELETE FROM sewn_products_warehouse;

-- 2. Удалить все записи из журнала ОТК
DELETE FROM qc_journal;

-- 3. Удалить все записи из склада готовой продукции (если нужно)
DELETE FROM finished_goods_warehouse;

-- 4. Проверка - должно показать 0 записей
SELECT
  (SELECT COUNT(*) FROM sewn_products_warehouse) as sewn_products_count,
  (SELECT COUNT(*) FROM qc_journal) as qc_journal_count,
  (SELECT COUNT(*) FROM finished_goods_warehouse) as finished_goods_count;

-- =====================================================
-- ГОТОВО! Все склады очищены.
-- =====================================================
